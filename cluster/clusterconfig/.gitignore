# Ignore all generated Talos config files (contain sensitive data)
# These are regenerated by talhelper genconfig during deployment
*.yaml
talosconfig

# Keep this .gitignore file so the directory exists after clone
!.gitignore
