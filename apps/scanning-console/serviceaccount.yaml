# Scanning Console - ServiceAccount
# Used by the scanning-console Deployment to interact with K8s API
# for managing scan pods across tool namespaces.
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: scanning-console
  namespace: scanning-console
  labels:
    app.kubernetes.io/name: scanning-console
    app.kubernetes.io/component: security-console
