apiVersion: v1
kind: Service
metadata:
  name: clamav
  namespace: clamav
  labels:
    app.kubernetes.io/name: clamav
spec:
  type: ClusterIP
  ports:
    - name: clamav
      port: 3310
      targetPort: 3310
      protocol: TCP
  selector:
    app.kubernetes.io/name: clamav
